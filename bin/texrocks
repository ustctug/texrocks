#!/usr/bin/env -S texlua
if os.getenv "LUA_PATH" then
    require 'texrocks.cmd'.main()
else
    local p = io.popen("lx --lua-version=5.3 lua --lua=" .. arg[0] .. " --no-loader -- " .. table.concat(arg, " "))
    if p then
        print(p:read("*a"))
        p:close()
    end
end
